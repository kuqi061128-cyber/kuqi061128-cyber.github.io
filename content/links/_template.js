/* ============================================================
 * 网站推荐模板（本文件不会被加载，复制一份作为新推荐）
 *
 * 新增一个推荐网站（三步）：
 *   1. 复制本文件到本目录，重命名（如 09-example.js），填写各项内容
 *   2. 在 index.html 底部「内容库引入区」加一行：
 *        <script src="content/links/09-example.js"></script>
 *   3. 保存刷新，卡片即出现在「网站推荐」分区
 *
 * 字段说明：
 *   name  网站名
 *   url   地址（https:// 开头，点击卡片新标签页打开）
 *   desc  一句话推荐理由
 *   tag   分类标签（如 AI / 开发 / 工具 / 社区 / 资源）
 *   color 可选：卡片字母图标的底色，不填自动轮换配色
 * ============================================================ */
registerSite({
  name: "示例网站",
  url: "https://example.com",
  desc: "一句话推荐理由。",
  tag: "工具",
  // color: "#38bdf8",
});

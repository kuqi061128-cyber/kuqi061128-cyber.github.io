/* ============================================================
 * 文章模板（本文件不会被加载，复制一份作为新文章）
 *
 * 发布步骤：
 *   1. 复制本文件到本目录，重命名为「日期-英文Slug.js」，
 *      如 2026-09-01-my-post.js
 *   2. 填写下面各项内容（id 改成没用过的数字）
 *   3. 在 index.html 底部「内容库引入区」加一行：
 *        <script src="content/posts/2026-09-01-my-post.js"></script>
 *   4. 保存刷新，文章即出现在列表里
 *
 * 字段说明：
 *   id        唯一编号（用没用过的数字；发布后不要再改，链接依赖它）
 *   title     标题
 *   date      发布日期，格式 YYYY-MM-DD（列表按它倒序排列）
 *   category  分类（右栏「分类统计」自动汇总）
 *   tags      标签数组（右栏「标签云」「热门文章」自动使用）
 *   views     初始浏览量基数（真实浏览会叠加在它之上）
 *   summary   摘要（显示在列表卡片上，一两句话）
 *   content   正文，支持 <p> <h3> <code> <img> 等标签；插图直接写裸文件名
 *            （如 <img src="1.png">），图片放在与本 js 同名的文件夹里
 * ============================================================ */
registerPost({
  id: 6,                       // ← 改成没用过的编号
  title: "我的新文章标题",
  date: "2026-09-01",
  category: "随笔",
  tags: ["随笔"],
  views: 0,
  summary: "这里写一两句话的摘要，会显示在文章列表卡片上。",
  content: `
    <p>这里写正文第一段。</p>
    <h3>小标题</h3>
    <p>正文支持常用标签：段落 p、小标题 h3、行内代码 code 等。</p>
  `,
});
